// Staging.xcconfig
API_BASE_URL = https:/$()/tvattstuga-api.vercel.app
INFOPLIST_KEY_APIBaseURL = $(API_BASE_URL)

// Optional local overrides (ignored by git): Staging.local.xcconfig
#include? "Staging.local.xcconfig"
